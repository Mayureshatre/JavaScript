"Use Strict"; //to use newer version of JS

// alert(3+3)   it works in the terminal of browser, but not in node JS


//code readability should be high like below
console.log(3+3);

console.log("Mayuresh");
// not like this,it will work but readability is poor
console.log(3
    +3);

let Name = "Mayuresh" // string in double quote
let age = 18 // number range about 2 to the opwer 53
let state; // undefined datatype, value is not assigned
let a=null; //null datatype, standalone value value is assigned but it is null 
    //bigint for number greater than 2 to the power of 53
    //boolean , true/false
    //symbol ,unique

//object
console.log(typeof null); // null is an object!!
console.log(typeof undefined);
